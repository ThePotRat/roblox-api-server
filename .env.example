# Server Configuration
PORT=3000
NODE_ENV=development

# Security Configuration (Optional)
# If API_KEY is not set, API runs without authentication
# Generate a strong random key for production: https://www.uuidgenerator.net/
API_KEY=your-secret-api-key-here

# Rate Limiting Configuration
# Window in milliseconds (60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000
# Maximum requests per window per IP
RATE_LIMIT_MAX_REQUESTS=100

# Cache Configuration (Optional - defaults are fine for most use cases)
# Cache TTL in seconds (300 = 5 minutes)
# CACHE_TTL=300

# API Timeout Configuration (Optional)
# External API request timeout in milliseconds
# API_TIMEOUT=10000

# Deployment-specific variables (set these in your hosting platform)
# For Railway, Render, Vercel, etc., these are often set automatically
# NODE_ENV=production
# PORT will be set by the hosting platform

# Example production values:
# NODE_ENV=production
# API_KEY=sk_live_abc123def456ghi789jkl012mno345pqr678stu901
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=1000
